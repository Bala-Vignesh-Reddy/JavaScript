//string it can be  written in single quote alos 
let name = "Bala"
console.log(name)
// .length  function can be used to find the length of the string
console.log(name.length)
//the string can be iterated with the indexes

//Template Literals
//these use backtics instead of quotes to define string
let friend = `Tavish`
console.log(friend)

let sentence = `${friend} is a friend of ${name}`
console.log(sentence)
//with Template Literals, it is possible  to use both single and double quotes inside a string
let string = `the name "is" Bala's`
console.log(string)

//We can insert variables directly in template literals. This is called String Interpolation


//Escape Sequence Characters
 //these are special like \', \n, \r, \t
